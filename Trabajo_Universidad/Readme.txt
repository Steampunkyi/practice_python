Para los cuatro ejercicios se ha generado una clase independiente. Luego se ha asignado a cada item su propia variable y para el calculo se definio un metodo en formula como se pide en el trabajo.

A continuacion se solicitan el registro de los items mediante el teclado y se agrega una variable para la clase a ejecutar.

Finalmente se imprime el total con la variable final.